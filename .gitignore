node_modules/
dist/
.env
*.log
.DS_Store

# Built APK artifacts — distributed via GitHub Releases, not the repo
*.apk
# Never commit signing material
*.keystore
*keystore*base64*
keystore.properties

# Local working files — not part of the app
# Claude skills installed for this project, and the design prompt
.claude/skills/
PROMPT_MAH_NOTES.md
